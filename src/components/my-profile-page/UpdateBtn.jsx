import { Button } from "@heroui/react";

const UpdateBtn = () => {
    return (
        <Button className="update-btn btn mt-5 bg-green-700 text-white">
            Update Profile
        </Button>
    );
};

export default UpdateBtn;