import { Button } from "@heroui/react";

const UpdateBtn = () => {
    return (
        <Button className="btn mt-5 bg-green-700 text-white">
            Update profile
        </Button>
    );
};

export default UpdateBtn;